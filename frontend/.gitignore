# macOS
.DS_Store

# Editor / tooling scratch
.vscode/
.idea/
*.swp

# Secrets — nothing in this repo needs them yet, but the agents will
.env
.env.*
!.env.example
*-service-account.json
gcp-credentials.json

# Node (web/.gitignore already covers web/node_modules and web/.next)
node_modules/

# Regenerated by `next dev` on every start. Ignored rather than committed so
# they don't show up as untracked noise after each run.
web/AGENTS.md
web/CLAUDE.md
